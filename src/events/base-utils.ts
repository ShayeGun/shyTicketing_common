export enum Keys {
    Default = "",
    TicketCreated = "ticket.create",
    TicketUpdated = "ticket.update",
}

export enum Exchanges {
    Default = "E1",
    Ticket = "ticket",
}

export enum Queues {
    Default = "",
    Ticket = "ticket",
}

